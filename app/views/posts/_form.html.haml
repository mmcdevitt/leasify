= form_for(@post) do |f|
  - if @post.errors.any?
    #error_explanation
      %h2
        = pluralize(@post.errors.count, "error")
        prohibited this post from being saved:
      %ul
        - @post.errors.full_messages.each do |msg|
          %li= msg
  %section.scrollable.wrapper.w-f
    .panel-body
      .add-page
        .row
          .col-md-8
            %h1 Post
            = f.text_area :title, id: "some-textarea", class: "wysihtml5 form-control"
            .actions
              = f.submit
